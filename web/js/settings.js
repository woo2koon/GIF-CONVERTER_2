const CONVERT_SVGS = {
    gif: {
        viewBox: "0 0 285.62 76.61",
        content: `
            <g fill="currentColor">
                <path d="M0,39.78C0,19.22,13.4,7.16,29.92,7.16c9.02,0,15.26,3.88,19.3,8.01l-6.66,7.92c-3.03-2.95-6.49-5.23-12.22-5.23-10.37,0-17.62,8.09-17.62,21.49s6.24,21.75,18.46,21.75c3.03,0,6.07-.76,7.84-2.36v-12.31h-11.04v-10.2h22.17v28.15c-4.3,4.13-11.46,7.5-20.23,7.5C12.81,71.89,0,60.6,0,39.78Z"/>
                <path d="M72.23,70.71h-12.47V8.34h12.47v62.37Z"/>
                <path d="M122.2,18.71h-25.96v16.27h22.17v10.45h-22.17v25.28h-12.47V8.34h38.43v10.37Z"/>
                <path d="M143.86,5.48h11.04v11.8h14.75V5.48h10.87v7.75h13.82V.34h11.13v57.31h-11.13v-18.88h-13.82v6.83h-36.66V5.48ZM154.23,76.61v-24.53h11.13v15.68h41.8v8.85h-52.93ZM154.9,36.75h14.75v-10.96h-14.75v10.96ZM194.35,29.92v-7.75h-13.82v7.75h13.82Z"/>
                <path d="M214.57,53.44l-1.18-8.51c5.48,0,11.72,0,18.46-.25v-4.47c-8.43-1.26-13.4-5.48-13.4-11.55,0-7.42,7.5-12.05,18.96-12.05s18.88,4.64,18.88,12.05c0,6.07-5.14,10.28-13.32,11.55v3.96c6.41-.42,12.31-.84,18.37-1.77l.84,7.59c-16.1,3.12-34.39,3.46-47.62,3.46ZM215.33,6.66h16.52V0h11.13v6.66h16.44v7.92h-44.08v-7.92ZM278.45,76.61h-54.28v-19.47h11.13v10.62h43.15v8.85ZM237.41,33.21c5.39,0,8.34-1.6,8.34-4.55s-2.95-4.64-8.34-4.64-8.43,1.6-8.43,4.64,3.03,4.55,8.43,4.55ZM275.84,34.81v26.13h-11.13V.25h11.13v25.45h9.78v9.1h-9.78Z"/>
            </g>
        `
    },
    mp4: {
        viewBox: "0 0 325.74 76.61",
        content: `
            <g fill="currentColor">
                <path d="M24.75,37.5c1.37,3.96,2.56,8.01,3.84,12.05h.43c1.37-4.04,2.47-8.09,3.84-12.05l10.5-29.16h14v62.36h-11.52v-26.04c0-5.81.85-14.66,1.54-20.48h-.34l-5.12,14.66-9.73,26.21h-7.25l-9.81-26.21-5.04-14.66h-.34c.68,5.82,1.62,14.66,1.62,20.48v26.04H0V8.34h14l10.75,29.16Z"/>
                <path d="M114.19,27.89c0,14.24-10.84,20.65-24.41,20.65h-8.45v22.16h-12.63V8.34h20.74c13.91,0,24.75,4.72,24.75,19.55ZM101.81,27.89c0-7.33-4.52-9.69-13.31-9.69h-7.17v20.48h7.6c8.62,0,12.89-3.62,12.89-10.79Z"/>
                <path d="M163.94,55.45h-8.02v15.25h-10.33v-15.25h-30.21v-9.44c9.98-12.89,19.54-26.04,29.53-38.93h11.01v38.85h8.02v9.52ZM145.76,21.15l-18.78,24.95h18.6v-19.64c0-.76.09-3.03.17-5.31Z"/>
                <path d="M182.54,5.48h11.18v11.8h14.94V5.48h11.01v7.75h14V.34h11.27v57.31h-11.27v-18.88h-14v6.83h-37.12V5.48ZM193.04,76.61v-24.52h11.27v15.68h42.33v8.85h-53.59ZM193.72,36.74h14.94v-10.96h-14.94v10.96ZM233.66,29.92v-7.75h-14v7.75h14Z"/>
                <path d="M253.8,53.43l-1.19-8.51c5.55,0,11.86,0,18.69-.25v-4.47c-8.53-1.26-13.57-5.48-13.57-11.55,0-7.42,7.6-12.05,19.2-12.05s19.12,4.64,19.12,12.05c0,6.07-5.21,10.28-13.48,11.55v3.96c6.49-.42,12.46-.84,18.6-1.77l.85,7.58c-16.3,3.12-34.82,3.46-48.22,3.46ZM254.57,6.66h16.73V0h11.27v6.66h16.64v7.92h-44.63v-7.92ZM318.49,76.61h-54.96v-19.47h11.26v10.62h43.7v8.85ZM276.93,33.2c5.46,0,8.45-1.6,8.45-4.55s-2.99-4.63-8.45-4.63-8.53,1.6-8.53,4.63,3.07,4.55,8.43,4.55ZM315.84,34.81v26.12h-11.27V.25h11.27v25.45h9.9v9.1h-9.9Z"/>
            </g>
        `
    },
    webp: {
        viewBox: "0 0 392.74 76.61",
        content: `
            <g fill="currentColor">
                <path d="M17.92,38.77c.94,6.74,1.96,13.15,2.9,19.89h.34c1.28-6.74,2.65-13.4,4.01-19.89l7.34-30.42h10.75l7.34,30.42c1.37,6.41,2.65,13.15,4.01,19.89h.43c.94-6.74,1.88-13.4,2.82-19.89l4.95-30.42h12.03l-11.69,62.36h-15.7l-7.08-30.84c-1.02-5.06-1.96-9.69-2.73-14.41h-.34c-.94,4.72-1.71,9.35-2.73,14.41l-6.91,30.84h-15.45L0,8.34h12.89l5.04,30.42Z"/>
                <path d="M118.88,18.71h-26.11v14.41h22.19v10.45h-22.19v16.69h27.05v10.45h-39.68V8.34h38.75v10.37Z"/>
                <path d="M172.22,23.68c0,5.56-3.24,11.71-8.62,13.48v.42c6.83,1.6,11.86,6.32,11.86,14.75,0,12.56-10.58,18.37-24.49,18.37h-22.53V8.34h20.91c12.97,0,22.87,3.62,22.87,15.34ZM159.93,25.54c0-5.48-3.75-7.67-11.35-7.67h-7.51v15.84h7.34c8.19,0,11.52-3.12,11.52-8.17ZM163.17,51.58c0-6.07-4.44-8.6-13.31-8.6h-8.79v18.12h8.79c8.62,0,13.31-3.03,13.31-9.52Z"/>
                <path d="M227.86,27.89c0,14.24-10.84,20.65-24.41,20.65h-8.45v22.16h-12.63V8.34h20.74c13.91,0,24.75,4.72,24.75,19.55ZM215.48,27.89c0-7.33-4.52-9.69-13.31-9.69h-7.17v20.48h7.6c8.62,0,12.89-3.62,12.89-10.79Z"/>
                <path d="M249.53,5.48h11.18v11.8h14.94V5.48h11.01v7.75h14V.34h11.27v57.31h-11.27v-18.88h-14v6.83h-37.12V5.48ZM260.03,76.61v-24.52h11.27v15.68h42.33v8.85h-53.59ZM260.71,36.74h14.94v-10.96h-14.94v10.96ZM300.65,29.92v-7.75h-14v7.75h14Z"/>
                <path d="M320.79,53.43l-1.2-8.51c5.55,0,11.86,0,18.69-.25v-4.47c-8.53-1.26-13.57-5.48-13.57-11.55,0-7.42,7.6-12.05,19.2-12.05s19.12,4.64,19.12,12.05c0,6.07-5.21,10.28-13.48,11.55v3.96c6.49-.42,12.46-.84,18.6-1.77l.85,7.58c-16.3,3.12-34.82,3.46-48.22,3.46ZM321.56,6.66h16.73V0h11.27v6.66h16.64v7.92h-44.63v-7.92ZM385.48,76.61h-54.96v-19.47h11.27v10.62h43.7v8.85ZM343.92,33.2c5.46,0,8.45-1.6,8.45-4.55s-2.99-4.63-8.45-4.63-8.53,1.6-8.53,4.63,3.07,4.55,8.43,4.55ZM382.84,34.81v26.12h-11.27V.25h11.27v25.45h9.9v9.1h-9.9Z"/>
            </g>
        `
    }
};

function syncUIToFile(fileObj) {
    if (!fileObj) return;
    const seg = getActiveSegment(fileObj);
    if (!seg) return;

    // Format Selection
    const formatValue = seg.format || "gif";
    const formatItems = document.querySelectorAll('#format-dropdown .dropdown-item');
    formatItems.forEach(item => {
        if (item.dataset.value === formatValue) {
            formatItems.forEach(i => i.classList.remove('active', 'font-bold'));
            item.classList.add('active', 'font-bold');
            const selectedSpan = document.getElementById('format-selected');
            if (selectedSpan) selectedSpan.textContent = item.textContent;
        }
    });

    // Update Conversion Button SVG
    const convertSvg = document.getElementById('convert-svg');
    if (convertSvg && CONVERT_SVGS[formatValue]) {
        const svgData = CONVERT_SVGS[formatValue];
        convertSvg.setAttribute('viewBox', svgData.viewBox);
        convertSvg.innerHTML = svgData.content;
    }

    // Format-dependent UI Visibility
    const groupGif = document.getElementById('format-group-gif');
    const containerAudio = document.getElementById('audio-toggle-container');
    const containerLoop = document.getElementById('loop-toggle-container');
    const containerDither = document.getElementById('dither-toggle-container');

    if (groupGif) {
        const showGif = (formatValue === 'gif');
        groupGif.classList.toggle('hidden', !showGif);
        groupGif.style.display = showGif ? 'block' : 'none';
    }
    if (containerDither) {
        const showDither = (formatValue === 'gif');
        containerDither.classList.toggle('hidden', !showDither);
        containerDither.style.display = showDither ? 'flex' : 'none';
    }
    if (containerLoop) {
        const showLoop = (formatValue === 'gif' || formatValue === 'webp');
        containerLoop.classList.toggle('hidden', !showLoop);
        containerLoop.style.display = showLoop ? 'flex' : 'none';
    }
    if (containerAudio) {
        const showAudio = (formatValue === 'mp4');
        containerAudio.classList.toggle('hidden', !showAudio);
        containerAudio.style.display = showAudio ? 'flex' : 'none';
    }

    // FPS
    const fpsSlider = document.getElementById('fps-slider');
    const fpsDisplay = document.getElementById('fps-display');
    const fpsSliderContainer = document.getElementById('fps-slider-container');
    const fpsDropdownContainer = document.getElementById('fps-dropdown-container');
    
    const currentFps = (seg.fps !== undefined) ? seg.fps : 24;

    if (formatValue === 'gif') {
        if (fpsSliderContainer) fpsSliderContainer.classList.remove('hidden');
        if (fpsDropdownContainer) fpsDropdownContainer.classList.add('hidden');
        
        const sliderVal = (currentFps === 0) ? 24 : currentFps; // Original is not for GIF slider
        fpsSlider.value = sliderVal;
        fpsDisplay.textContent = `${sliderVal} FPS`;
    } else {
        if (fpsSliderContainer) fpsSliderContainer.classList.add('hidden');
        if (fpsDropdownContainer) fpsDropdownContainer.classList.remove('hidden');
        
        // Update Dropdown UI
        const fpsItems = document.querySelectorAll('#fps-dropdown .dropdown-item');
        fpsItems.forEach(item => {
            const val = parseInt(item.dataset.value);
            const isActive = (val === currentFps);
            item.classList.toggle('active', isActive);
            item.classList.toggle('font-bold', isActive);
            if (isActive) {
                document.getElementById('fps-selected').textContent = item.textContent;
            }
        });
        
        fpsDisplay.textContent = currentFps === 0 ? "원본 유지" : `${currentFps} FPS`;
    }

    // Speed
    const speedSlider = document.getElementById('speed-slider');
    const speedDisplay = document.getElementById('speed-display');
    const speed = seg.speed || 1.0;
    if (speedSlider) speedSlider.value = speed;
    if (speedDisplay) speedDisplay.textContent = `${parseFloat(speed).toFixed(1)}x`;

    // Update preset buttons
    document.querySelectorAll('.speed-preset-btn').forEach(btn => {
        const btnSpeed = parseFloat(btn.dataset.speed);
        if (Math.abs(btnSpeed - speed) < 0.01) {
            btn.classList.remove('bg-white', 'border-slate-200', 'text-slate-500');
            btn.classList.add('bg-indigo-50', 'border-indigo-200', 'text-indigo-600');
        } else {
            btn.classList.add('bg-white', 'border-slate-200', 'text-slate-500');
            btn.classList.remove('bg-indigo-50', 'border-indigo-200', 'text-indigo-600');
        }
    });

    // Live preview speed
    const mainPlayer = document.getElementById('main-player');
    if (mainPlayer) {
        mainPlayer.playbackRate = speed;
    }
    if (window.selectedFileObj && window.selectedFileObj.isYoutube && window.ytPlayer) {
        if (typeof window.ytPlayer.setPlaybackRate === 'function') {
            window.ytPlayer.setPlaybackRate(speed);
        }
    }

    // Resolution
    const resValue = seg.resolution || "중간 (720p)";
    const resItems = document.querySelectorAll('#res-dropdown .dropdown-item');
    resItems.forEach(item => {
        if (item.dataset.value === resValue) {
            resItems.forEach(i => i.classList.remove('active'));
            item.classList.add('active');
            document.getElementById('res-selected').textContent = item.textContent;
        }
    });
    
    const customResContainer = document.getElementById('custom-res-container');
    if (resValue === "직접 설정") {
        document.getElementById('custom-width').value = seg.customWidth || fileObj.width;
        document.getElementById('custom-height').value = seg.customHeight || fileObj.height;
        const lockBtn = document.getElementById('aspect-ratio-lock');
        const isLocked = seg.aspectRatioLock !== undefined ? seg.aspectRatioLock : true;
        lockBtn.dataset.active = isLocked ? 'true' : 'false';
        
        const icon = lockBtn.querySelector('.material-symbols-outlined');
        if (icon) icon.textContent = isLocked ? 'link' : 'link_off';
        
        lockBtn.classList.toggle('text-indigo-600', isLocked);
        lockBtn.classList.toggle('text-slate-300', !isLocked);
        customResContainer.classList.remove('hidden');
    } else {
        customResContainer.classList.add('hidden');
    }

    // Colors
    const colorValue = seg.numColors || 256;
    const colorItems = document.querySelectorAll('#colors-dropdown .dropdown-item');
    colorItems.forEach(item => {
        if (parseInt(item.dataset.value) === colorValue) {
            colorItems.forEach(i => i.classList.remove('active'));
            item.classList.add('active');
            document.getElementById('colors-selected').textContent = item.textContent;
        }
    });

    // Toggles
    updateToggleUI(document.getElementById('loop-toggle'), seg.loopPlayback !== undefined ? seg.loopPlayback : true);
    updateToggleUI(document.getElementById('dither-toggle'), seg.useDither !== undefined ? seg.useDither : false);
    updateToggleUI(document.getElementById('audio-toggle'), seg.includeAudio !== undefined ? seg.includeAudio : true);
    
    // Batch Sync
    updateToggleUI(document.getElementById('batch-sync-toggle'), fileObj.isBatchSync || false);
    updateSyncToggleVisibility();

    // Crop State
    const cropToggle = document.getElementById('crop-toggle');
    const cropControlsContainer = document.getElementById('crop-controls-container');
    if (seg.crop) {
        window.isCropMode = true;
        window.cropBoxState = { ...seg.crop };
        updateToggleUI(cropToggle, true);
        cropControlsContainer.classList.remove('hidden');
        cropControlsContainer.classList.add('flex');
    } else {
        window.isCropMode = false;
        window.cropBoxState = { x: 10, y: 10, w: 80, h: 80 };
        updateToggleUI(cropToggle, false);
        cropControlsContainer.classList.add('hidden');
        cropControlsContainer.classList.remove('flex');
    }
    updateCropUI();

    
    // 키프레임 목록 업데이트
    if (window.updateKeyframeListUI) {
        window.updateKeyframeListUI();
    }
}

function updateCropOverlaySize() {
    const video = document.getElementById('main-player');
    const overlay = document.getElementById('crop-overlay');
    const container = document.getElementById('video-container');
    if (!overlay || !container) return;

    let vWidth, vHeight;
    if (window.selectedFileObj && window.selectedFileObj.isYoutube) {
        vWidth = window.selectedFileObj.width;
        vHeight = window.selectedFileObj.height;
    } else {
        if (!video || video.videoWidth === 0) return;
        vWidth = video.videoWidth;
        vHeight = video.videoHeight;
    }

    const containerWidth = container.clientWidth;
    const containerHeight = container.clientHeight;
    const videoRatio = vWidth / vHeight;
    const containerRatio = containerWidth / containerHeight;

    let displayWidth, displayHeight;
    if (videoRatio > containerRatio) {
        displayWidth = containerWidth;
        displayHeight = containerWidth / videoRatio;
    } else {
        displayHeight = containerHeight;
        displayWidth = containerHeight * videoRatio;
    }

    overlay.style.width = `${displayWidth}px`;
    overlay.style.height = `${displayHeight}px`;
    overlay.style.left = `${(containerWidth - displayWidth) / 2}px`;
    overlay.style.top = `${(containerHeight - displayHeight) / 2}px`;
}

function updateCropUI() {
    const cropBox = document.getElementById('crop-box');
    const cropOverlay = document.getElementById('crop-overlay');
    const pixelDisplay = document.getElementById('crop-pixel-display');

    if (!window.isCropMode) {
        cropOverlay.classList.add('hidden');
        cropBox.classList.add('hidden');
        return;
    }
    cropOverlay.classList.remove('hidden');
    cropBox.classList.remove('hidden');
    cropBox.style.left = `${window.cropBoxState.x}%`;
    cropBox.style.top = `${window.cropBoxState.y}%`;
    cropBox.style.width = `${window.cropBoxState.w}%`;
    cropBox.style.height = `${window.cropBoxState.h}%`;

    // Update Custom Resolution Inputs and Pixel HUD to match crop box pixels
    if (window.selectedFileObj) {
        const realW = Math.round(window.selectedFileObj.width * (window.cropBoxState.w / 100));
        const realH = Math.round(window.selectedFileObj.height * (window.cropBoxState.h / 100));
        
        if (pixelDisplay) {
            pixelDisplay.textContent = `${realW} × ${realH}`;
        }

        const customWInput = document.getElementById('custom-width');
        const customHInput = document.getElementById('custom-height');
        if (customWInput && customHInput) {
            customWInput.value = realW;
            customHInput.value = realH;
            
            // Also sync to segment data
            const seg = getActiveSegment(window.selectedFileObj);
            if (seg) {
                seg.customWidth = realW;
                seg.customHeight = realH;
            }
        }
    }
}



function initCropLogic() {
    const cropToggle = document.getElementById('crop-toggle');
    const cropControlsContainer = document.getElementById('crop-controls-container');
    const cropBox = document.getElementById('crop-box');
    const cropOverlay = document.getElementById('crop-overlay');

    cropToggle.addEventListener('click', () => {
        if (!window.selectedFileObj) return;
        window.isCropMode = !window.isCropMode;
        
        const seg = getActiveSegment(window.selectedFileObj);
        if (window.isCropMode) {
            seg.crop = { ...window.cropBoxState };
            cropControlsContainer.classList.remove('hidden');
            cropControlsContainer.classList.add('flex');
        } else {
            seg.crop = null;
            cropControlsContainer.classList.add('hidden');
            cropControlsContainer.classList.remove('flex');
        }
        updateToggleUI(cropToggle, window.isCropMode);
        updateCropUI();
    });

    // Crop Reset
    const resetBtn = document.getElementById('crop-reset-btn');
    if (resetBtn) {
        resetBtn.addEventListener('click', () => {
            // 박스 및 비율 데이터 초기화
            window.cropBoxState = { x: 10, y: 10, w: 80, h: 80 };
            window.cropAspectRatio = 'free';
            
            const seg = getActiveSegment(window.selectedFileObj);
            if (seg) {
                seg.crop = { ...window.cropBoxState };
                seg.cropAspectRatio = 'free';
                if (typeof updateAutoKeyframe === 'function') updateAutoKeyframe();
            }
            updateCropUI();
            
            // 드롭다운 UI 상태 동기화
            const selectedText = document.getElementById('crop-ratio-selected');
            if (selectedText) selectedText.textContent = "자유형 (Free)";
            
            const dropdown = document.getElementById('crop-ratio-dropdown');
            if (dropdown) {
                const items = dropdown.querySelectorAll('.dropdown-item');
                items.forEach(item => {
                    if (item.dataset.value === 'free') {
                        item.classList.add('active');
                    } else {
                        item.classList.remove('active');
                    }
                });
            }
        });
    }

    // Ratio Dropdown
    const ratioDropdown = document.getElementById('crop-ratio-dropdown');
    if (ratioDropdown) {
        ratioDropdown.addEventListener('change', (e) => {
            window.cropAspectRatio = e.detail.value;
            applyRatioToBox();
            if (typeof updateAutoKeyframe === 'function') updateAutoKeyframe();
        });
    }

    // Rotate Ratio
    const rotateBtn = document.getElementById('rotate-ratio-btn');
    if (rotateBtn) {
        rotateBtn.addEventListener('click', () => {
            if (!window.cropAspectRatio || window.cropAspectRatio === 'free') return;
            
            // Clean the ratio string: take only the part before any space or parenthesis
            let cleanRatio = window.cropAspectRatio.split(' ')[0].trim();
            const parts = cleanRatio.split(':');
            
            if (parts.length === 2) {
                const newRatio = `${parts[1]}:${parts[0]}`;
                window.cropAspectRatio = newRatio;
                
                // Update the current segment's state
                const seg = getActiveSegment(window.selectedFileObj);
                if (seg) {
                    seg.cropAspectRatio = newRatio;
                    if (typeof updateAutoKeyframe === 'function') updateAutoKeyframe();
                }

                applyRatioToBox();
                
                // Update dropdown text: search DOM carefully
                const selectedText = document.getElementById('crop-ratio-selected');
                const dropdown = document.getElementById('crop-ratio-dropdown');
                const options = dropdown ? dropdown.querySelectorAll('.custom-option') : [];
                
                let foundName = null;
                for (let opt of options) {
                    const optVal = (opt.getAttribute('data-value') || opt.dataset.value || "").trim();
                    if (optVal === newRatio) {
                        // Use innerText and trim to get clean text without extra whitespace/newlines
                        foundName = (opt.innerText || opt.textContent).trim();
                        break;
                    }
                }

                // Fallback mapping (Must match index.html EXACTLY)
                if (!foundName) {
                    const fallbacks = {
                        "free": "자유형 (Free)",
                        "16:9": "16:9 (HD 와이드)",
                        "1:1": "1:1 (정사각형)",
                        "4:3": "4:3 (표준)",
                        "21:9": "21:9 (시네마틱)",
                        "4:5": "4:5 (인스타그램)",
                        "3:2": "3:2 (사진)",
                        "2.35:1": "2.35:1 (시네마틱)"
                    };
                    foundName = fallbacks[newRatio];
                }

                if (selectedText) {
                    selectedText.textContent = foundName ? foundName : `${newRatio} (회전됨)`;
                }
            }
        });
    }

    // Dragging State
    let isDragging = false;
    let dragMode = null; // 'move' or handle position like 'top-left'
    let startX, startY, startBox;

    const onMouseDown = (e) => {
        if (!window.isCropMode) return;
        
        const handle = e.target.closest('.crop-handle');
        const box = e.target.closest('#crop-box');
        
        e.preventDefault();
        e.stopPropagation(); // Prevent interaction from reaching the player
        isDragging = true;
        startX = e.clientX;
        startY = e.clientY;
        
        const overlay = document.getElementById('crop-overlay');
        const rect = overlay.getBoundingClientRect();

        if (handle) {
            dragMode = handle.dataset.handle;
            startBox = { ...window.cropBoxState };
        } else if (box) {
            dragMode = 'move';
            startBox = { ...window.cropBoxState };
        } else {
            // Clicked on empty overlay -> Start drawing new box
            dragMode = 'draw';
            const x = ((e.clientX - rect.left) / rect.width) * 100;
            const y = ((e.clientY - rect.top) / rect.height) * 100;
            window.cropBoxState = { x, y, w: 0, h: 0 };
            startBox = { ...window.cropBoxState };
            updateCropUI();
        }
        
        document.addEventListener('mousemove', onMouseMove);
        document.addEventListener('mouseup', onMouseUp);
    };

    const onMouseMove = (e) => {
        if (!isDragging || !window.isCropMode) return;
        

        const overlay = document.getElementById('crop-overlay');
        const rect = overlay.getBoundingClientRect();
        const dx = ((e.clientX - startX) / rect.width) * 100;
        const dy = ((e.clientY - startY) / rect.height) * 100;
        
        let nextState = { ...startBox };
        
        if (dragMode === 'move') {
            nextState.x = Math.max(0, Math.min(100 - startBox.w, startBox.x + dx));
            nextState.y = Math.max(0, Math.min(100 - startBox.h, startBox.y + dy));
        } else if (dragMode === 'draw') {
            if (dx > 0) {
                nextState.w = Math.min(100 - startBox.x, dx);
            } else {
                nextState.x = Math.max(0, startBox.x + dx);
                nextState.w = startBox.x - nextState.x;
            }
            
            if (dy > 0) {
                nextState.h = Math.min(100 - startBox.y, dy);
            } else {
                nextState.y = Math.max(0, startBox.y + dy);
                nextState.h = startBox.y - nextState.y;
            }
        } else {
            // Resize logic
            if (dragMode.includes('r')) nextState.w = Math.max(2, Math.min(100 - startBox.x, startBox.w + dx));
            if (dragMode.includes('b')) nextState.h = Math.max(2, Math.min(100 - startBox.y, startBox.h + dy));
            if (dragMode.includes('l')) {
                const newW = Math.max(2, Math.min(startBox.x + startBox.w, startBox.w - dx));
                nextState.x = startBox.x + (startBox.w - newW);
                nextState.w = newW;
            }
            if (dragMode.includes('t')) {
                const newH = Math.max(2, Math.min(startBox.y + startBox.h, startBox.h - dy));
                nextState.y = startBox.y + (startBox.h - newH);
                nextState.h = newH;
            }
        }

        // Apply Aspect Ratio if locked (except during simple 'move')
        if (dragMode !== 'move' && window.cropAspectRatio && window.cropAspectRatio !== 'free') {
            const parts = window.cropAspectRatio.split(':');
            const ratio = parseFloat(parts[0]) / parseFloat(parts[1]);
            const overlayRatio = rect.width / rect.height; // Matches videoRatio
            const targetRatio = ratio / overlayRatio;

            if (dragMode === 'draw') {
                nextState.h = nextState.w / targetRatio;
                if (dy < 0) nextState.y = startBox.y - nextState.h;
            } else {
                const isPureL = dragMode === 'l';
                const isPureR = dragMode === 'r';
                const isPureT = dragMode === 't';
                const isPureB = dragMode === 'b';

                if (isPureL || isPureR) {
                    // 가로 변만 드래그 → 높이를 비율에 맞게, 세로 중앙 기준으로 조정
                    const newH = nextState.w / targetRatio;
                    const centerY = startBox.y + startBox.h / 2;
                    nextState.h = newH;
                    nextState.y = centerY - newH / 2;
                } else if (isPureT || isPureB) {
                    // 세로 변만 드래그 → 너비를 비율에 맞게, 가로 중앙 기준으로 조정
                    const newW = nextState.h * targetRatio;
                    const centerX = startBox.x + startBox.w / 2;
                    nextState.w = newW;
                    nextState.x = centerX - newW / 2;
                } else {
                    // 코너 드래그 (기존 방식 유지)
                    if (dragMode.includes('l') || dragMode.includes('r')) {
                        nextState.h = nextState.w / targetRatio;
                        if (dragMode.includes('t')) nextState.y = (startBox.y + startBox.h) - nextState.h;
                    } else {
                        nextState.w = nextState.h * targetRatio;
                        if (dragMode.includes('l')) nextState.x = (startBox.x + startBox.w) - nextState.w;
                    }
                }
            }

            // Safety boundaries
            if (nextState.x < 0) { nextState.x = 0; nextState.w = startBox.x + startBox.w; nextState.h = nextState.w / targetRatio; }
            if (nextState.y < 0) { nextState.y = 0; nextState.h = startBox.y + startBox.h; nextState.w = nextState.h * targetRatio; }
            if (nextState.x + nextState.w > 100) { nextState.w = 100 - nextState.x; nextState.h = nextState.w / targetRatio; }
            if (nextState.y + nextState.h > 100) { nextState.h = 100 - nextState.y; nextState.w = nextState.h * targetRatio; }
        }
        
        window.cropBoxState = nextState;
        const seg = getActiveSegment(window.selectedFileObj);
        if (seg) {
            seg.crop = { ...window.cropBoxState };
            // Auto-key logic: If already has keyframes, update/add at current time
            if (typeof updateAutoKeyframe === 'function') {
                updateAutoKeyframe();
            }
        }
        updateCropUI();
    };

    const onMouseUp = () => {
        isDragging = false;
        document.removeEventListener('mousemove', onMouseMove);
        document.removeEventListener('mouseup', onMouseUp);
    };

    function applyRatioToBox() {
        if (!window.cropAspectRatio || window.cropAspectRatio === 'free') return;
        const parts = window.cropAspectRatio.split(':');
        const ratio = parseFloat(parts[0]) / parseFloat(parts[1]);
        
        const overlay = document.getElementById('crop-overlay');
        const rect = overlay.getBoundingClientRect();
        if (rect.width === 0) return;

        const overlayRatio = rect.width / rect.height;
        const targetRatio = ratio / overlayRatio;

        let w = 80;
        let h = w / targetRatio;
        
        if (h > 80) {
            h = 80;
            w = h * targetRatio;
        }
        
        window.cropBoxState = {
            x: (100 - w) / 2,
            y: (100 - h) / 2,
            w: w,
            h: h
        };
        
        const seg = getActiveSegment(window.selectedFileObj);
        if (seg) seg.crop = { ...window.cropBoxState };
        updateCropUI();
    }

    cropOverlay.addEventListener('mousedown', onMouseDown);
    cropOverlay.addEventListener('click', (e) => e.stopPropagation());
}
